version: "3.9"

services:
  m2m-translator:
    build: .
    container_name: m2m-translator
    ports:
      - "8085:8085"
    restart: always
    volumes:
      - hf_cache:/root/.cache/huggingface

volumes:
  hf_cache:
